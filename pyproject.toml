[project]
name = "agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "victorgonbu", email = "victormanuelgonzale@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "ddgs>=9.16.0",
    "gradio>=6.26.0",
    "litellm>=1.100.0",
    "python-dotenv",
    "pytz>=2026.3.post1",
    "smolagents>=1.26.0",
]

[project.scripts]
agents = "agents:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
